import Testing
@testable import DevToolsKitFeatureFlags

@Suite("FeatureFlagStore")
struct FeatureFlagStoreTests {
    @Test func registersFlags() async throws {
        // Placeholder — tests will be added in Phase 4
    }
}
